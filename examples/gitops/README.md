# GitOps Examples

These examples show how to install the operator from a pinned release and apply a `Maintenance` resource through GitOps.

Use these as starting points. Review namespace, target Ingress name, and release tag before applying them in a real cluster.

- [Argo CD](argocd/application.yaml)
- [Flux](flux/kustomization.yaml)

